import React, { Component } from 'react';import SliderItem from './SliderItem';import './index.css';let timer = null;export default class Slider extends Component{    state = {        nowLocal: 0,    };    componentDidMount(){        const { nowLocal } = this.state;        const { autoPlay } = this.props;        if(autoPlay) {            this.autoPlay(nowLocal);        }    }    autoPlay = (nowLocal) => {        const { delay, imgData } = this.props;        timer = setTimeout(() => {            nowLocal++;            if(nowLocal >= imgData.length ){                nowLocal = nowLocal - imgData.length            }            this.setState({nowLocal});            this.autoPlay(nowLocal);        }, delay*1000)    };    render(){        const { imgData, speed } = this.props;        const { nowLocal } = this.state;        console.log(nowLocal);        return (            <div className='slider'>                <ul                    style={{                        left: -100 * nowLocal + "%",                        // transitionDuration: speed + "s",                        width: imgData.length * 100 + "%"                    }}                >                    {imgData.map((item, index) => <SliderItem item={item} count={imgData.length} key={index} />)}                </ul>            </div>        )    }}