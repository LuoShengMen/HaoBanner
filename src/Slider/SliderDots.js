import React, { Component } from 'react';export default class SliderDots extends Component{    render(){        let { imgData, nowLocal, } = this.props;        return (            <div className="dots">                {                    imgData.map((item, index) => {                        return (                            <div className="dot-item" key={index}></div>                        )                    })                }            </div>        )    }}