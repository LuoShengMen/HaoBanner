import React, {Component} from 'react';export default class SliderDots extends Component {  showDot = (index) => {    this.props.showDot(index);  }  render() {    let {imgData, nowLocal,} = this.props;    return (        <div className="dots">          {            imgData.map((item, index) => {              return (                  <div className={nowLocal === index ? 'dot-item dot' : 'dot-item'} key={index}                       onMouseOver={() => this.showDot(index)}></div>              )            })          }        </div>    )  }}