import React, { Component } from 'react';export default class Slider extends Component{    render(){        let { count, item, nowLocal, index, fadein, seamless } = this.props;        let width = 100 / count + '%';        if(seamless) {            return (                <li className="seamless" style={{width: width}}>                    <img src={item.src} alt={item.alt} />                </li>            )        } else {            return (                    !!fadein ? (                        <div className={nowLocal === index ? 'fadein-item fadein' : 'fadein-item'}>                            <img src={item.src} alt={item.alt} />                        </div>                    ) : (                        <div className={nowLocal === index ? 'slider-item' : 'slider-item slid'}>                            <img src={item.src} alt={item.alt} />                        </div>                    )            )        }    }}